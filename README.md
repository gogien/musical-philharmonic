# Musical Philharmonic - Система управления филармонией

Веб-приложение для управления билетной системой филармонии, включающее управление концертами, билетами, пользователями и статистику продаж.

## 📋 Содержание

- [Технологический стек](#технологический-стек)
- [Архитектура проекта](#архитектура-проекта)
- [Backend](#backend)
- [Frontend](#frontend)
- [Безопасность](#безопасность)
- [База данных](#база-данных)
- [Установка и запуск](#установка-и-запуск)
- [API Документация](#api-документация)

## 🛠 Технологический стек

### Backend
- **Java 17** - язык программирования
- **Spring Boot 4.0.0** - фреймворк для создания приложений
- **Spring Security 7.0.0** - безопасность и аутентификация
- **Spring Data JPA** - работа с базой данных через ORM
- **Hibernate** - реализация JPA
- **PostgreSQL** - реляционная база данных
- **JWT (JSON Web Tokens)** - аутентификация на основе токенов
  - `jjwt-api` 0.11.5
  - `jjwt-impl` 0.11.5
  - `jjwt-jackson` 0.11.5
- **SpringDoc OpenAPI 2.6.0** - документация API (Swagger)
- **Maven** - управление зависимостями и сборка проекта
- **Jakarta Validation** - валидация данных

### Frontend
- **HTML5** - разметка
- **CSS3** - стилизация (кастомные стили)
- **Vanilla JavaScript (ES6+)** - клиентская логика
  - Модульная архитектура с классами
  - Fetch API для HTTP-запросов
  - Динамическая генерация UI

## 🏗 Архитектура проекта

Проект следует **многослойной архитектуре (Layered Architecture)** с разделением на следующие слои:

```
┌─────────────────────────────────────┐
│         Presentation Layer          │
│    (REST Controllers + Frontend)    │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│          Business Layer             │
│           (Services)                │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│          Data Access Layer          │
│        (Repositories/JPA)           │
└─────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────┐
│         Database Layer              │
│         (PostgreSQL)                 │
└─────────────────────────────────────┘
```

### Принципы архитектуры:
- **Separation of Concerns** - разделение ответственности между слоями
- **Dependency Injection** - внедрение зависимостей через Spring
- **RESTful API** - REST-архитектура для API
- **Stateless Authentication** - JWT-токены для аутентификации
- **DTO Pattern** - использование Data Transfer Objects для передачи данных

## 🔧 Backend

### Структура пакетов

```
org.app.musical_philharmonic/
├── config/              # Конфигурация приложения
├── controller/          # REST контроллеры
├── dto/                 # Data Transfer Objects
├── entity/              # JPA сущности
├── exception/           # Обработка исключений
├── repository/          # JPA репозитории
├── security/            # Безопасность и JWT
├── service/             # Бизнес-логика
└── util/                # Утилиты
```

### REST Controllers

#### 1. **AuthController** (`/api/auth`)
- `POST /api/auth/register` - регистрация нового пользователя
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/logout` - выход из системы
- `GET /api/auth/me` - получение информации о текущем пользователе

#### 2. **ConcertController** (`/api/concerts`)
- `POST /api/concerts` - создание концерта (ADMIN)
- `GET /api/concerts/{id}` - получение концерта по ID
- `PUT /api/concerts/{id}` - обновление концерта (ADMIN)
- `DELETE /api/concerts/{id}` - удаление концерта (ADMIN)
- `POST /api/concerts/search` - поиск концертов с фильтрацией и пагинацией (ADMIN)
- `GET /api/concerts/public/{id}` - публичный доступ к концерту
- `POST /api/concerts/public/upcoming` - предстоящие концерты (публичный)
- `GET /api/concerts/{id}/stats` - статистика по концерту (ADMIN)

#### 3. **TicketController** (`/api/tickets`)
- `POST /api/tickets/search` - поиск билетов с фильтрацией (ADMIN, CASHIER)
- `GET /api/tickets/{id}` - получение билета по ID
- `POST /api/tickets` - продажа билета (CASHIER)
- `DELETE /api/tickets/{id}` - удаление билета (ADMIN)
- `POST /api/tickets/{id}/return` - возврат билета (CASHIER)
- `GET /api/tickets/availability/{concertId}` - доступные билеты для концерта

#### 4. **UserController** (`/api/users`)
- `POST /api/users/search` - поиск пользователей с фильтрацией (ADMIN)
- `GET /api/users/{id}` - получение пользователя по ID (ADMIN)
- `POST /api/users` - создание пользователя (ADMIN)
- `PUT /api/users/{id}` - обновление пользователя (ADMIN)
- `DELETE /api/users/{id}` - удаление пользователя (ADMIN)

#### 5. **HallController** (`/api/halls`)
- `POST /api/halls/search` - список залов с пагинацией (ADMIN)
- `GET /api/halls/{id}` - получение зала по ID (ADMIN)
- `POST /api/halls` - создание зала (ADMIN)
- `PUT /api/halls/{id}` - обновление зала (ADMIN)
- `DELETE /api/halls/{id}` - удаление зала (ADMIN)
- `GET /api/halls/public/{id}` - публичный доступ к залу

#### 6. **PerformerController** (`/api/performers`)
- `POST /api/performers/search` - поиск исполнителей (ADMIN)
- `GET /api/performers/{id}` - получение исполнителя по ID (ADMIN)
- `POST /api/performers` - создание исполнителя (ADMIN)
- `PUT /api/performers/{id}` - обновление исполнителя (ADMIN)
- `DELETE /api/performers/{id}` - удаление исполнителя (ADMIN)
- `GET /api/performers/public/{id}` - публичный доступ к исполнителю
- `POST /api/performers/public/list` - список исполнителей (публичный)

#### 7. **CustomerController** (`/api/customer`)
- `POST /api/customer/concerts` - концерты для клиента
- `POST /api/customer/tickets/book` - бронирование билетов
- `POST /api/customer/tickets/purchase` - покупка забронированных билетов
- `GET /api/customer/tickets` - билеты клиента

#### 8. **StatisticsController** (`/api/statistics`)
- `GET /api/statistics` - системная статистика (ADMIN, CASHIER)
  - Общее количество пользователей
  - Среднее время сессии клиента
  - Статистика по билетам
  - Распределение пользователей по ролям
  - Распределение билетов по статусам

#### 9. **AboutController** (`/api/about`)
- `GET /api/about` - информация о проекте и авторе

#### 10. **IndexController**
- `GET /` - обслуживание главной страницы

### Services (Бизнес-логика)

#### 1. **ConcertService**
- Управление концертами (CRUD операции)
- Поиск и фильтрация концертов
- Расчет доступных билетов

#### 2. **TicketService**
- Управление билетами
- Бронирование и продажа билетов
- Возврат билетов
- Поиск билетов по концерту, покупателю, статусу
- Проверка доступности билетов

#### 3. **UserService**
- Управление пользователями (CRUD)
- Поиск пользователей
- Управление ролями

#### 4. **HallService**
- Управление залами (CRUD)
- Список залов с пагинацией

#### 5. **PerformerService**
- Управление исполнителями (CRUD)
- Поиск исполнителей

#### 6. **StatisticsService**
- Расчет системной статистики
- Среднее время сессии пользователей
- Распределение данных по категориям

#### 7. **UserDetailsServiceImpl**
- Реализация Spring Security UserDetailsService
- Загрузка пользователей для аутентификации

### DTOs (Data Transfer Objects)

#### Request DTOs:
- **AuthRequest** - запрос на вход (email, password)
- **RegisterRequest** - запрос на регистрацию (email, password, name, phone)
- **ConcertRequest** - создание/обновление концерта
- **ConcertSearchRequest** - поиск концертов (title, date, performerId, hallId)
- **TicketBookRequest** - бронирование билетов
- **TicketPurchaseRequest** - покупка билетов
- **TicketSellRequest** - продажа билетов кассиром
- **TicketSearchRequest** - поиск билетов (concertName, buyerEmail, status)
- **TicketReturnRequest** - возврат билета
- **UserCreateRequest** - создание пользователя
- **UserUpdateRequest** - обновление пользователя
- **UserSearchRequest** - поиск пользователей (name, email, role)
- **HallRequest** - создание/обновление зала
- **PerformerRequest** - создание/обновление исполнителя
- **PerformerSearchRequest** - поиск исполнителей
- **SalesHistoryRequest** - история продаж
- **PageableRequest** - базовый запрос с пагинацией

#### Response DTOs:
- **AuthResponse** - ответ аутентификации (token, name, role)
- **ConcertResponse** - информация о концерте
- **TicketResponse** - информация о билете
- **UserResponse** - информация о пользователе
- **HallResponse** - информация о зале
- **PerformerResponse** - информация об исполнителе

### Entities (JPA Сущности)

#### 1. **User**
- `id` (UUID) - уникальный идентификатор
- `email` (String) - email пользователя (уникальный)
- `name` (String) - имя пользователя
- `phone` (String) - телефон
- `password` (String) - хешированный пароль
- `role` (Role enum) - роль пользователя
- `createdAt` (LocalDateTime) - дата создания

#### 2. **Concert**
- `id` (Integer) - уникальный идентификатор
- `title` (String) - название концерта
- `date` (LocalDate) - дата концерта
- `time` (LocalTime) - время концерта
- `ticketPrice` (BigDecimal) - цена билета
- `hall` (Hall) - зал проведения
- `performer` (Performer) - исполнитель

#### 3. **Ticket**
- `id` (Integer) - уникальный идентификатор
- `concert` (Concert) - концерт
- `buyer` (User) - покупатель
- `seatNumber` (String) - номер места
- `status` (TicketStatus enum) - статус билета
- `purchaseTimestamp` (LocalDateTime) - время покупки
- `reservationExpiration` (LocalDateTime) - срок действия бронирования
- `paymentMethod` (String) - способ оплаты
- `returnReason` (String) - причина возврата
- `returnTime` (LocalDateTime) - время возврата

#### 4. **Hall**
- `id` (Integer) - уникальный идентификатор
- `name` (String) - название зала
- `capacity` (Integer) - вместимость
- `location` (String) - местоположение

#### 5. **Performer**
- `id` (Integer) - уникальный идентификатор
- `name` (String) - имя исполнителя

#### 6. **UserSession**
- `id` (UUID) - уникальный идентификатор
- `user` (User) - пользователь
- `loginTime` (LocalDateTime) - время входа
- `logoutTime` (LocalDateTime) - время выхода

#### Enums:
- **Role**: `ADMIN`, `CASHIER`, `CUSTOMER`
- **TicketStatus**: `AVAILABLE`, `RESERVED`, `SOLD`, `RETURNED`

### Repositories

Все репозитории расширяют `JpaRepository` и используют Spring Data JPA:

- **UserRepository** - методы поиска по email, роли, имени
- **ConcertRepository** - методы поиска по залу, исполнителю, названию, дате
- **TicketRepository** - методы поиска по концерту, покупателю, статусу
  - Поддерживает `JpaSpecificationExecutor` для динамических запросов
- **HallRepository** - методы поиска по названию, вместимости
- **PerformerRepository** - методы поиска по имени
- **UserSessionRepository** - методы поиска сессий пользователей

## 🎨 Frontend

### Структура

```
src/main/resources/static/
├── index.html          # Главная страница
├── 403.html            # Страница ошибки 403
├── 404.html            # Страница ошибки 404
├── 500.html            # Страница ошибки 500
├── css/
│   └── main.css        # Основные стили
└── js/
    ├── app.js          # Главный класс приложения
    ├── views.js        # Компоненты представлений
    ├── datatable.js    # Компонент таблицы данных
    └── validation.js   # Валидация форм
```

### Основные компоненты

#### 1. **App** (`app.js`)
Главный класс приложения, отвечающий за:
- Управление состоянием (текущий пользователь, роль, представление)
- API-клиент для взаимодействия с backend
- Навигация между страницами
- Аутентификация и авторизация
- Уведомления пользователю
- Загрузка различных представлений

**Основные методы:**
- `checkAuth()` - проверка аутентификации
- `apiCall()` - универсальный метод для API-запросов
- `render()` - отрисовка текущего представления
- `loadWelcomePage()` - главная страница
- `loadStatisticsPage()` - страница статистики
- `loadAboutPage()` - страница "О проекте"
- `showNotification()` - отображение уведомлений

#### 2. **ViewLoader** (`views.js`)
Класс для загрузки и управления представлениями:
- Загрузка таблиц данных для админа
- Формы создания/редактирования
- Представления для разных ролей (CUSTOMER, CASHIER, ADMIN)
- CRUD операции для всех сущностей

**Основные методы:**
- `load()` - загрузка представления по имени
- `loadConcertsTable()` - таблица концертов
- `loadTicketsTable()` - таблица билетов
- `loadUsersTable()` - таблица пользователей
- `loadHallsTable()` - таблица залов
- `loadPerformersTable()` - таблица исполнителей
- Методы редактирования и сохранения для каждой сущности

#### 3. **DataTable** (`datatable.js`)
Переиспользуемый компонент таблицы данных:
- Пагинация
- Сортировка по колонкам
- Фильтрация и поиск
- CRUD операции (создание, редактирование, удаление)
- Адаптивная отрисовка данных

**Основные методы:**
- `render()` - отрисовка таблицы
- `loadData()` - загрузка данных с сервера
- `handleFilter()` - обработка фильтров
- `editItem()` - редактирование записи
- `deleteItem()` - удаление записи

#### 4. **FormValidator** (`validation.js`)
Валидация форм на клиенте:
- Проверка обязательных полей
- Валидация email
- Валидация дат и времени
- Валидация чисел
- Валидация телефонов
- Отображение ошибок валидации

### Роли и представления

#### **CUSTOMER (Клиент)**
- Главная страница с концертами
- Просмотр деталей концерта
- Бронирование билетов
- Покупка забронированных билетов
- Просмотр своих билетов
- Профиль пользователя

#### **CASHIER (Кассир)**
- Продажа билетов
- Просмотр билетов
- История продаж
- Статистика

#### **ADMIN (Администратор)**
- Управление концертами (CRUD)
- Управление билетами (просмотр, удаление)
- Управление пользователями (CRUD)
- Управление залами (CRUD)
- Управление исполнителями (CRUD)
- Статистика системы
- Доступ к Swagger UI

### UI/UX Особенности

- **Адаптивный дизайн** - поддержка различных размеров экрана
- **Темная тема** - темный фон с акцентными цветами
- **Модальные окна** - для форм создания/редактирования
- **Уведомления** - всплывающие уведомления об успехе/ошибках
- **Таблицы с фильтрацией** - поиск и фильтрация по колонкам
- **Пагинация** - навигация по страницам данных
- **Валидация форм** - клиентская валидация с отображением ошибок

## 🔒 Безопасность

### Аутентификация

Приложение использует **JWT (JSON Web Tokens)** для stateless аутентификации:

1. **Регистрация/Вход:**
   - Пользователь отправляет credentials
   - Сервер проверяет и генерирует JWT токен
   - Токен сохраняется в HTTP-only cookie
   - Токен содержит: email (subject), role (claim), expiration

2. **Защита эндпоинтов:**
   - `JwtAuthenticationFilter` проверяет токен в каждом запросе
   - Токен извлекается из cookie или Authorization header
   - Валидация токена и загрузка пользователя
   - Установка SecurityContext для доступа к данным пользователя

3. **Выход:**
   - Запись времени окончания сессии в базу данных
   - Очистка JWT cookie

### Авторизация

Используется **Role-Based Access Control (RBAC)** с тремя ролями:

#### **ADMIN**
- Полный доступ ко всем операциям
- Управление всеми сущностями (CRUD)
- Доступ к статистике
- Доступ к Swagger UI

#### **CASHIER**
- Продажа билетов
- Возврат билетов
- Просмотр билетов и истории продаж
- Доступ к статистике
- Нет доступа к управлению концертами, пользователями и т.д.

#### **CUSTOMER**
- Просмотр концертов (публичный доступ)
- Бронирование билетов
- Покупка забронированных билетов
- Просмотр своих билетов
- Редактирование своего профиля

### Конфигурация безопасности

**SecurityConfig** настраивает:
- Отключение CSRF (для REST API)
- Stateless сессии
- Публичные эндпоинты (авторизация, публичные данные)
- Защищенные эндпоинты с проверкой ролей
- Обработка ошибок аутентификации и авторизации
- JWT фильтр в цепочке безопасности

### Защита данных

- **Пароли:** хешируются с помощью BCrypt
- **JWT Secret:** настраивается через `application.yml`
- **Валидация:** Jakarta Validation на уровне DTO
- **SQL Injection:** защита через JPA/Hibernate
- **XSS:** валидация и санитизация входных данных

## 🗄 База данных

### Схема базы данных

#### Таблицы:

1. **Users**
   - `id` (UUID, PK)
   - `email` (VARCHAR, UNIQUE, NOT NULL)
   - `name` (VARCHAR)
   - `phone` (VARCHAR)
   - `password` (VARCHAR, NOT NULL)
   - `role` (VARCHAR, NOT NULL)
   - `created_at` (TIMESTAMP, NOT NULL)

2. **Halls**
   - `id` (INTEGER, PK, AUTO_INCREMENT)
   - `name` (VARCHAR, NOT NULL)
   - `capacity` (INTEGER, NOT NULL)
   - `location` (VARCHAR)

3. **Performers**
   - `id` (INTEGER, PK, AUTO_INCREMENT)
   - `name` (VARCHAR, NOT NULL)

4. **Concerts**
   - `id` (INTEGER, PK, AUTO_INCREMENT)
   - `title` (VARCHAR, NOT NULL)
   - `date` (DATE, NOT NULL)
   - `time` (TIME, NOT NULL)
   - `ticket_price` (DECIMAL, NOT NULL)
   - `hall_id` (INTEGER, FK → Halls.id)
   - `performer_id` (INTEGER, FK → Performers.id)

5. **Tickets**
   - `id` (INTEGER, PK, AUTO_INCREMENT)
   - `concert_id` (INTEGER, FK → Concerts.id, NOT NULL)
   - `buyer_id` (UUID, FK → Users.id)
   - `seat_number` (VARCHAR, NOT NULL)
   - `status` (VARCHAR, NOT NULL)
   - `purchase_timestamp` (TIMESTAMP, NOT NULL)
   - `reservation_expiration` (TIMESTAMP)
   - `payment_method` (VARCHAR)
   - `return_reason` (VARCHAR)
   - `return_time` (TIMESTAMP)

6. **user_sessions**
   - `id` (UUID, PK)
   - `user_id` (UUID, FK → Users.id, NOT NULL)
   - `login_time` (TIMESTAMP, NOT NULL)
   - `logout_time` (TIMESTAMP)

### Связи

- **Concert** → **Hall** (Many-to-One)
- **Concert** → **Performer** (Many-to-One)
- **Ticket** → **Concert** (Many-to-One)
- **Ticket** → **User** (Many-to-One, опционально)
- **UserSession** → **User** (Many-to-One)

### Миграции

База данных управляется через Hibernate с `ddl-auto: update`, что автоматически создает/обновляет схему при запуске приложения.

## 🚀 Установка и запуск

### Требования

- Java 17 или выше
- Maven 3.6+
- PostgreSQL 12+
- Node.js (опционально, для разработки)

### Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE philharmonic;
```

2. Настройте подключение в `application.yml` или через переменные окружения:
```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/philharmonic
    username: postgres
    password: postgres
```

Или через переменные окружения:
```bash
export DB_URL=jdbc:postgresql://localhost:5432/philharmonic
export DB_USERNAME=postgres
export DB_PASSWORD=postgres
```

### Запуск приложения

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd musical_philharmonic
```

2. Соберите проект:
```bash
mvn clean install
```

3. Запустите приложение:
```bash
mvn spring-boot:run
```

Или через IDE, запустив класс `MusicalPhilharmonicApplication`.

4. Откройте браузер:
```
http://localhost:8080
```

### Первый запуск

При первом запуске Hibernate автоматически создаст все таблицы в базе данных. Для создания администратора используйте API или добавьте пользователя напрямую в базу данных.

## 📚 API Документация

### Swagger UI

После запуска приложения API документация доступна по адресу:
```
http://localhost:8080/swagger-ui.html
```

**Доступ:** Только для пользователей с ролью ADMIN

### Основные эндпоинты

#### Публичные эндпоинты (не требуют аутентификации):
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/about` - информация о проекте
- `GET /api/concerts/public/{id}` - публичная информация о концерте
- `POST /api/concerts/public/upcoming` - предстоящие концерты
- `GET /api/performers/public/{id}` - публичная информация об исполнителе
- `POST /api/performers/public/list` - список исполнителей
- `GET /api/halls/public/{id}` - публичная информация о зале

#### Защищенные эндпоинты:
Все остальные эндпоинты требуют аутентификации через JWT токен.

### Формат запросов/ответов

Все API используют JSON формат:
- **Content-Type:** `application/json`
- **Accept:** `application/json`

### Аутентификация

JWT токен передается через:
1. **Cookie** (предпочтительно): `JWT=<token>`
2. **Authorization Header**: `Authorization: Bearer <token>`

### Примеры запросов

#### Регистрация:
```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "name": "Иван Иванов",
    "phone": "+7 (999) 123-45-67"
  }'
```

#### Вход:
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

#### Создание концерта (требует ADMIN):
```bash
curl -X POST http://localhost:8080/api/concerts \
  -H "Content-Type: application/json" \
  -H "Cookie: JWT=<token>" \
  -d '{
    "title": "Концерт классической музыки",
    "date": "2024-12-25",
    "time": "19:00",
    "hallId": 1,
    "performerId": 1,
    "ticketPrice": 1500.00
  }'
```

## 📝 Дополнительная информация

### Обработка ошибок

Все ошибки обрабатываются через `GlobalExceptionHandler`:
- Валидация данных
- Ошибки аутентификации
- Ошибки авторизации
- Общие исключения

Формат ответа об ошибке:
```json
{
  "timestamp": "2024-12-20T22:37:31",
  "status": 400,
  "error": "Validation Failed",
  "message": "Проверьте правильность введенных данных",
  "fieldErrors": {
    "email": "Email должен быть валидным"
  }
}
```

### Логирование

Приложение использует SLF4J для логирования. Все ошибки логируются с полным stack trace.

### Производительность

- Использование пагинации для больших наборов данных
- Lazy loading для связанных сущностей
- Индексы в базе данных для часто используемых полей
- Кэширование не используется (можно добавить при необходимости)

## 👤 Автор

**Гогин Григорий Андреевич**
- Группа: ИД23-3
- Email: gogingrisha@gmail.com
- Телефон: +7 (916) 583-94-76

## 📄 Лицензия

Этот проект создан в учебных целях.

